-- LazyVim bootstrap. Leader keys, options, keymaps and autocmds are loaded by
-- LazyVim from lua/config/*.lua (options before plugins, keymaps/autocmds on VeryLazy).
-- When running inside VS Code (vim.g.vscode), LazyVim's vscode extra trims the plugin set.
require("config.lazy")
